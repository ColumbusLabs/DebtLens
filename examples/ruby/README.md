# Calibrated Ruby language-pack fixture for DebtLens.

Run with:

```bash
debtlens scan examples/ruby --pack ruby --min-severity info
```

This fixture intentionally triggers all four Ruby core rules:

- `ruby-todo-comment`
- `ruby-duplicate-logic`
- `ruby-dead-abstraction`
- `ruby-large-function`
