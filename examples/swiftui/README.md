# SwiftUI example

Oversized SwiftUI screen fixture for the `swiftui` pack (`swiftui-large-view`, `swiftui-state-sprawl`).

```bash
debtlens scan examples/swiftui --pack swiftui --min-severity info
```

Pair with `--pack swift,swiftui` when one scan should include core Swift maintainability rules and SwiftUI UI debt.
