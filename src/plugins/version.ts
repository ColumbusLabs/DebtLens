/**
 * Plugin API version supported by this DebtLens release. Bumped on breaking
 * changes to the `Detector` / `DetectorContext` contract; the bump policy is
 * documented in CHANGELOG.md and docs/plugin-api-rfc.md.
 */
export const DEBTLENS_PLUGIN_API_VERSION = 1;
